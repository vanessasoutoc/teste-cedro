<%= bootstrap_form_for @disk do |form| %>

<%= form.collection_select :restaurant_id, Restaurant.all, :id, :name, {prompt: 'Selecione um Restaurante'}, { :multiple => false } %>

<%= form.text_field :name, id: :disk_name, label: 'Prato'  %>

<%= form.text_field :price, id: :disk_price, label: 'Preço' %>

<%= form.submit 'Salvar',class: 'btn btn-primary pull-right' %>

<% end %>
<%= link_to('Cancelar', disks_path, class: 'btn btn-default') %>